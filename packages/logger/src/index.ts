/**
 * @f1t/logger - Placeholder
 * 
 * This package will contain SQLite persistence for telemetry data.
 * Currently disabled due to better-sqlite3 compatibility issues with Node.js 25.
 * 
 * To implement:
 * 1. Use native node:sqlite (Node 22+) or switch to sqlite3 package
 * 2. Port the schema, writers, and lap detector from the original implementation
 * 3. Wire up to PacketEmitter from @f1t/listener
 */

export const PLACEHOLDER = true;
